import { makeStyles } from '@material-ui/styles';

export default makeStyles(theme => ({
  root: {
    flex: 3,
    display: 'flex',
    position: 'relative',
    flexDirection: 'column',
    alignItems: 'center'
  }
}));
